////演習問題

// RPGのクリティカルヒットを再現
// 比較演算子 == > < >= <= !=

// スライムと戦っている。
// 1から10のサイコロをふって、
// 6未満：サイコロの目だけダメージを与えたと表示。
// 6以上：クリティカルヒットとして、100のダメージを与えたと表示。
//
// 追加サイコロ(値 1か2)を振る
//その値を「追加のサイコロは、X」と表示
// 1の場合：「追加攻撃!スライムに10のダメージを与えた」と表示。
// 2の場合：「追加攻撃に失敗した」と表示。
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(void){
    srand((unsigned int)time(NULL));
    int hit = rand() % 10 + 1;
    int x = rand() % 2 + 1;   //追加のサイコロｘ 
    //printf("%d\n", hit);
    if (hit < 6) {
        printf("スライムに、%dのダメージを与えた\n", hit);
    } else {
        printf("クリティカルヒット!スライムに、100のダメージを与えた\n");
    }
    
    if(x==1){
        printf("追加のサイコロは、%d\n追加攻撃!スライムに、10のダメージを与えた",x);
        }
    else {
        printf("追加のサイコロは、%d\n追加攻撃に失敗した",x);
    }
}
